name: Clang-Format Check

on:
  pull_request:
    branches:
      - main

jobs:
  clang-format:
    runs-on: ubuntu-latest

    steps:
      - name: Install clang-format 22
        run: |
          sudo apt-get update
          sudo apt-get install -y wget xz-utils

          wget https://github.com/llvm/llvm-project/releases/download/llvmorg-22.1.0-rc3/LLVM-22.1.0-rc3-Linux-X64.tar.xz > /dev/null

          sudo mkdir -p /opt/llvm-22

          tar -xf LLVM-22.1.0-rc3-Linux-X64.tar.xz
          sudo mv LLVM-22.1.0-rc3-Linux-X64 /opt/llvm-22

          echo "/opt/llvm-22/LLVM-22.1.0-rc3-Linux-X64/bin" >> $GITHUB_PATH
          export PATH=/opt/llvm-22/LLVM-22.1.0-rc3-Linux-X64/bin:$PATH

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run clang-format check
        run: |
          FILES=$(git ls-files '*.cxx' '*.hxx' '*.txx')

          if [ -z "$FILES" ]; then
            echo "no file to check."
            exit 0
          fi

          clang-format -i $FILES

          # If file change -> error
          if ! git diff --exit-code; then
            echo "::error::Format error."
            exit 1
          fi

          echo "Clang-format OK."